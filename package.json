{
  "type": "module",
  "scripts": {
    "test-vitest": "vitest run --dir vitest",
    "test-jest": "jest"
  },
  "devDependencies": {
    "vitest": "0.34.4",
    "jest": "^29.7.0"
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/jest.setup.cjs"
    ],
    "testMatch": [
      "<rootDir>/*.test.jest.js"
    ]
  }
}
